//! `sito-core`
//!
//! Core contracts, shared data structures, verdicts (Block, Allow, Rewrite, Forward),
//! error definitions, and fundamental traits for the sito DNS server pipeline.

#[cfg(test)]
mod tests {
    #[test]
    fn test_core_initialization() {
        assert_eq!(2 + 2, 4);
    }
}
